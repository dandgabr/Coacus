"""Coacus engine core: generators, validators, dispatcher and governor.

The engine is closed for modification (OCP): it operates on data registries
(frontmatter, harness.json, handler conventions). Extend the framework by
adding data, not by changing this package.
"""
