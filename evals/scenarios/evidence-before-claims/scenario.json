{
  "id": "evidence-before-claims",
  "title": "Evidence before claiming completion",
  "harness": "opencode",
  "prompt": "In at most two short lines and without editing any file: before claiming that a change is complete or passing, what must you do first?",
  "checks": [
    { "kind": "regex", "value": "(?i)(run|execute)[^.]{0,40}(command|test|verif)" },
    { "kind": "regex", "value": "(?i)(evidence|output|confirm|before)[^.]{0,50}claim|claim[^.]{0,40}(evidence|verif)" },
    { "kind": "not_contains", "value": "I have verified" }
  ],
  "rubric": [
    "The reply reflects 'evidence before claims': run the verification command and read its output first.",
    "It does not assert a passing state that was never measured.",
    "No file was modified."
  ],
  "acceptance": "The model states that a completion claim requires running the verification command and quoting its output."
}
