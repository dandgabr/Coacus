{
  "name": "cursor",
  "bootstrap": {
    "supported": false,
    "shape": "A",
    "reason": "Cursor has a sessionStart hook (project .cursor/hooks.json or user ~/.cursor/hooks.json) that emits snake_case additional_context and cannot block. Render stays deferred until a live acceptance test is possible; Coacus does not use the legacy hooks-cursor.json filename.",
    "hook_path": ".cursor/hooks.json",
    "native_key": "additional_context"
  },
  "detection": ["CURSOR_TRACE_ID"],
  "tool_mapping": {},
  "tool_denylist": [],
  "install": {
    "method": "Cursor Plugins: .cursor-plugin/plugin.json; local testing under ~/.cursor/plugins/local/<name>/, or distribute via a marketplace",
    "target": "plugin marketplace"
  }
}
